# 執筆スタイルガイド

このサイトのページを追加・編集するときの決まりごとです．

## 1. 句読点

<!-- 以下 2 か所は句読点そのものを引用しているため，一括変換の対象外 -->
**全角の「，」と「．」を使います．「、」と「。」は使いません．**
理工系の学会誌にあわせた表記で，サイト全体でこれに統一しています．

```
○  当研究室では，在庫管理や交通信号制御を対象としています．
×  当研究室では、在庫管理や交通信号制御を対象としています。
```

### 例外（半角のまま，変換しない）

| 対象 | 例 |
|---|---|
| コードブロック・インラインコード | `` `np.array([1, 2, 3])` `` |
| URL | `https://example.com/a,b` |
| 英文 | `inventory, production, and transportation.` |
| 数値の小数点 | `3.14` |
| YAML のリスト区切り | `categories: [卒業式, 受賞]` |

とくに **YAML の区切りは必ず半角 `,`** です．全角「，」を書くと区切りとして認識されず，
`[卒業式，受賞]` は「卒業式，受賞」という 1 つの項目になってしまいます．

### 変換しないもの

- 中黒「・」はそのまま（例：在庫・生産・物流）
- `join/iist/index.qmd` 内のコメントアウトされた中国語は，中国語の「。」が正しいのでそのまま

## 2. 言語

ページの**読者**で言語を決めます．サイト全体を二重化（日本語版／英語版）はしません．

| 層 | ページ | 言語 |
|---|---|---|
| ① 全体向け | `index`，`research/*`，`people`，`contact` | **日英併記** |
| ② 日本語話者向け | `join/b3`，`join/m1`，`thesis`，`lab`，`blog/*`，`news/*` | 日本語のみ |
| ③ 海外向け | `join/iist` | 英語のみ |

新しいページを作るときは，まず「これは誰が読むか」を決めてから層を選びます．

### ① 併記ページの書き方

- **見出し**は `日本語 | English` の形（例：`## アクセス | Access`）
- **本文**は日本語の段落 → 英語の段落の順に並べる．段落数を揃えると対応が分かりやすい
- **タブ（`.panel-tabset`）は使わない** — 英語が隠れて検索結果に出ず，読者にも気づかれにくいため

### `lang` の指定

サイト既定は `_quarto.yml` の `lang: ja` です．
③ の英語のみのページは front matter で上書きします．

```yaml
---
title: "..."
lang: en
---
```

## 3. Markdown / Quarto の注意点

過去に実際に壊れた箇所です．

- **生 HTML の行頭にインデントを入れない．** 4 スペース以上でコードブロック扱いになり，
  タグがそのまま画面に表示されます
- **カードなどの `<a>` は 1 行に収める．** 複数行に分けると Pandoc が Markdown として
  再解釈し，末尾に空の `<a>` が生成されます
- Pandoc は連続するインライン要素を `<p>` で包みます．grid / flex の直接の子にしたい場合は
  CSS 側で `> p { display: contents }` を当てます（`scss/_shared.scss` に実例あり）
- **改行は行末に `\`** を置きます．ただの改行は連結されます
- 画像を SCSS からのみ参照する場合は `_quarto.yml` の `resources:` に追加します
  （Quarto のリンク走査では見つからないため）

## 4. スタイルの実装

- `scss/_shared.scss` — 明暗テーマ共通のルール（ここを直せば両方に反映）
- `scss/custom.scss` — ライトテーマの変数と固有ルール
- `scss/dark_custom.scss` — ダークテーマの変数と固有ルール

配色は「彩度の高い色は 1 色だけ」という方針です．

| 変数 | ライト | ダーク | 用途 |
|---|---|---|---|
| `$primary` | `#1e293b` | `#38bdf8` | ナビバー，ホバー枠．前に出ない色 |
| `$accent` | `#0e7490` | `#22d3ee` | ボタン，見出しの縦バー，タグ．**唯一の彩色** |
| `$on-accent` | `#ffffff` | `#0f172a` | 上記の塗りに乗せる文字色 |

`$on-accent` を分けているのは，ダークテーマの `$accent` が明るく，白文字だと
コントラストが 1.8:1 まで落ちて読めなくなるためです（ダーク文字なら 9.9:1）．

色を変えるときは，この 3 変数に加えて `images/og-image.png` と `favicon.svg` も
揃えてください．`favicon.ico` と `apple-touch-icon.png` は `favicon.svg` から
`_tools/make-favicon.sh` で生成するので，手で描き直す必要はありません．

研究テーマの図（`research/<テーマ>/*.svg`）は，`news/` と同じで **記事と同じ
フォルダに置き，front matter からファイル名だけで参照** します．

```yaml
image: rl.svg        # ○ research/RL/rl.svg
image: /images/...   # × 別の場所に置くと管理が分かれる
```

この 4 枚は背景を透明にし，線を `#1596b4` の 1 色で描いています．カード自身の
背景が透けるので，1 枚で明暗どちらのテーマでも成立します（`<img>` 参照なので
`currentColor` は使えません）．この色は両テーマの `$accent` の中間で，どちらの
カード背景に対しても 3:1 以上を確保しています．

トップページのヒーロー（`index.qmd` 冒頭のインライン SVG）は `currentColor` で
描いているので，`.hero-figure` の `color: $accent` 一箇所で両テーマに追従します．
色を変えても描き直す必要はありません．

`images/og-image.png`（SNS カード）は手描きではなく `og-image.src.html` から
生成します．再生成のコマンドは同ファイル冒頭のコメントにあります．

`favicon.svg` はこのヒーローと同じ図（等高線と最適解）を，16px で残るところまで
削ったものです．研究テーマの図と違って**文字は使いません**．3 文字の "ISL" は
タブの 16px では 1 文字あたり 4px 強にしかならず，読めないためです．

### 使い回している 2 つの部品

新しい要素を足すときは，まずこの 2 つで足りないか考えてください．

| 部品 | 意味 | 使う場所 |
|---|---|---|
| **アクセントバー** | 構造 | セクション見出し（`h2` / `h3`）と，ヒーローの所属行（横向き）だけ |
| **チップ** | 属性 | 教員の肩書，学生の学年，ニュースの分類，Join Us の対象者，学位論文の受賞 |

アクセントバーは**セクション見出しだけ**の印です．1 つの印を 2 つの階層に使うと，
階層を示すどころか潰してしまうため，次の 2 箇所では意図的に外しています．

- **ページタイトル（`.quarto-title-banner`）** — 横帯の地色と境界線ですでに区別が
  ついています．バーを付けると `People` と `教員 | Faculty` が同じ見え方になります
- **footer の所属行** — 同じバーの中に 4 本の見出し罫がすでにあります

リスト項目の見出しも見出しではないので，`h3:not(.listing-title)` で除外しています．

チップは `_shared.scss` の `@mixin chip($fg)` が唯一の定義です．Quarto が自分で
出力するもの（`.listing-category`，名簿カードの `description`）も，見た目を真似た
別のピルを作らずにこの mixin を呼んでいます．本文中で使うときは `.chip` クラス：

```markdown
1. クラスタリングによるLIMEの精度向上と安定化，佐藤克海 <span class="chip">優秀論文賞受賞</span>
```

塗りつぶしボタン（`.btn-primary`）は**そのページの主要な操作**にだけ使います．
現状はトップの Join Us と Contact のメールアドレスの 2 箇所だけです．
